f = open('test_input.txt')

m = re.compile('^([A-Z])([0-9]+)$')
for l in f:
    l = l.rstrip()
    g = m.match(m)
    action = g.group(0)
    distance = g.group(1)

    print("%s: %s" % (action, distance))
    
    
